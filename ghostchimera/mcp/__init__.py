"""MCP (Model Context Protocol) package.

Note: This implements GHOST's internal tool protocol over HTTP
(action/discover/call on 127.0.0.1:3100). It is NOT the standard
Model Context Protocol JSON-RPC over stdio. The naming is historical;
see server.py for protocol details.
"""
